node_modules
out
dist
build
coverage
.dev-data
*.local
.DS_Store
# Instaladores generados por electron-builder
release
release-private
# Dataset REAL: sólo en el PC del autor y en sus copias de seguridad, nunca en
# el repositorio (es público). Ver docs/DATASET-REAL.md.
resources/real-data
